Start the program by running main.ts. Resize the terminal window as you see fit!

Commands:

- `help` - tells you more about existing commands
- `echo <message>` - prints a message you add
- `image <prompt>` - generates an ascii image in terminal based on a prompt
  - uses the _____ public image-gen API
- `exit` - exits the program
